---
description: "Start the task lifecycle from tasks.json using the spectral:execute-task skill"
---

Tell your human partner you are invoking the "spectral execute-task" skill to load tasks from tasks.json, let them choose one, and run the lifecycle for that selected task.
